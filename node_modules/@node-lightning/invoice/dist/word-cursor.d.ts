/// <reference types="node" />
export declare class WordCursor {
    words: number[];
    position: number;
    constructor(words?: number[]);
    get wordsRemaining(): number;
    writeUIntBE(val: number, wordLen: number): void;
    writeBytes(buf: Buffer, pad?: boolean): void;
    readUIntBE(numWords: number): number;
    readBytes(numWords: number, pad?: boolean): Buffer;
    private _merge;
}
